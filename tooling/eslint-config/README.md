# @virtality/eslint-config

Shared ESLint flat configs used across the monorepo.

## Available Configs

- `@virtality/eslint-config/next`: Next.js + TypeScript lint rules.
- `@virtality/eslint-config/shared-ui`: Canonical Shared UI Import enforcement for promoted components (`PROMOTED_COMPONENTS`).
